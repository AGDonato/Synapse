# ==================== SYNAPSE BACKEND - CONFIGURAÇÃO ====================
#
# ⚠️  IMPORTANTE: ESTE BACKEND NÃO ESTÁ IMPLEMENTADO ⚠️
#
# Este arquivo faz parte do diretório /backend que existe fisicamente
# mas representa um backend Node.js que foi REMOVIDO do projeto.
#
# O projeto atual é um FRONTEND APENAS (React + TypeScript) funcionando
# com mock data. Este arquivo serve apenas como REFERÊNCIA para 
# desenvolvimento futuro de backend.
#
# Status atual: ❌ Backend removido | ✅ Frontend maduro (256+ arquivos)
# ==========================================================================

# ==================== DATABASE (FUTURO) ====================
# PostgreSQL configuration for when backend is implemented
DATABASE_URL="postgresql://synapse_user:synapse_pass@localhost:5432/synapse"

# ==================== JWT (FUTURO) ====================
# JWT configuration for authentication
JWT_SECRET="your-super-secret-jwt-key-change-in-production"
JWT_EXPIRES_IN="7d"
JWT_REFRESH_EXPIRES_IN="30d"

# ==================== SERVER (FUTURO) ====================
# Server configuration
PORT=8080
NODE_ENV=development
CORS_ORIGIN="http://localhost:5173"

# ==================== REDIS CACHE (FUTURO) ====================
# Redis for caching and session management
REDIS_URL="redis://localhost:6379"
CACHE_TTL=600

# ==================== FILE UPLOAD (FUTURO) ====================
# File upload configuration
UPLOAD_MAX_SIZE=10485760
UPLOAD_PATH="./uploads"
ALLOWED_FILE_TYPES="pdf,doc,docx,jpg,jpeg,png"

# ==================== SECURITY (FUTURO) ====================
# Security settings
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100
BCRYPT_ROUNDS=12

# ==================== WEBSOCKET (FUTURO) ====================
# WebSocket for real-time features
WS_ENABLED=true
WS_CORS_ORIGIN="http://localhost:5173"

# ==================== LOGGING (FUTURO) ====================
# Logging configuration
LOG_LEVEL=info
LOG_FILE="./logs/app.log"

# ==================== EMAIL (FUTURO) ====================
# Email configuration for notifications
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
EMAIL_FROM="noreply@synapse.local"

# ==========================================================================
# NOTAS IMPORTANTES:
#
# 1. Este backend NÃO ESTÁ IMPLEMENTADO no projeto atual
# 2. O diretório /backend existe mas foi mantido apenas como referência
# 3. O projeto funciona exclusivamente no frontend com mock data
# 4. Para roadmap de implementação futura, consulte:
#    - docs/FUTURE_BACKEND.md
#    - docs/CURRENT_STATUS.md
#    - docs/BACKEND_MIGRATION.md (histórico)
#
# 5. O frontend maduro (256+ arquivos TypeScript) está preparado para 
#    receber um backend real quando implementado
#
# Status: 📋 Referência para Desenvolvimento Futuro
# Última atualização: Janeiro 2025
# ==========================================================================